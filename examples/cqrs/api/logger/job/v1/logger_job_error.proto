syntax = "proto3";

package logger.job.v1;
import "errors/errors.proto";

option go_package = "logger/job/v1;v1";
option java_multiple_files = true;

enum LoggerJobErrorReason {
    option (errors.default_code) = 500;

    UNKNOWN_ERROR = 0;
}

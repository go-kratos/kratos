syntax = "proto3";

package kratos.demo.codes;

// 多语言特定包名，用于源代码引用
option go_package = ".;codes";
option java_multiple_files = true;
option java_package = "com.github.kratos.demo.codes";
option objc_class_prefix = "KratosDemoCodes";

enum Kratos {
    RequestBlocked = 0;     // 请求已被封禁
    MissingField = 1;       // 请求参数缺失
}
